/.phpunit.cache
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
# Written by the setup wizard when it finishes. It must never ship inside the package, or a
# buyer's fresh copy would look already installed and the wizard would never run.
/storage/installed
/vendor
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
auth.json
npm-debug.log
yarn-error.log
/.fleet
/.idea
/.vscode
